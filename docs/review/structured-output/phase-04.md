# Structured output phase 04 handoff

Base: phase 03 checkpoint `7aefcb57`. Candidate: `b3e99d5476b3b807492ffe7c9544967bd09a62f3`.

New auto/required jobs capture a versioned queued policy and operation closure. Preflight resolves advertised capability and operator verification outside campaign locks, rechecks provider identity, and freezes the full selection before any provider reservation. Resume uses the saved selection without rediscovery. Historical jobs with no marker retain legacy serialization and configuration identity.

Every structured invocation uses the checked serialized body and its hash. The private ledger separates primary, choice repair, continuity review/repair, event extension and scene rewrite; exempt assessments retain their existing behavior. Reservations, dispatch and response completion persist independently. Completed/dispatched work without a usable result stops for recovery rather than redispatching. Explicit review Retry rotates the logical attempt identity transactionally; worker lease attempts do not authorize extra calls. Local fact-format repair remains local.

Repository saves lock and merge protected stored fields before validating and writing the same merged value. Reads and final locked commits reject incompatible contracts, altered producing hashes and future envelopes. Explicit Keep requires the same protected producing evidence and performs no new provider call. Private failure evidence retains exact prepared bytes/hash and bounded observed model, provider, response identity and partial content. Missing observations remain unknown.

New modes impose a shared 1,000,000 UTF-16-character prepared-request ceiling before reservation/dispatch so any dispatched failure remains durably recordable. Oversized requests stop recoverably with guidance to reduce included context or shorten input. Legacy limits are unchanged. This ceiling is a deliberate compatibility limitation, not a token estimate or silent truncation.

Arbitrary tracker updates survive in a new-mode-only private `acceptedTrackerUpdateEvidence` sidecar. Existing normalized campaign trackers and legacy snapshot shapes stay unchanged. Raw tracker evidence does not enter narration, review, Chronicle or illustration projections.

## Verification

- Focused evidence-limit/executor tests: 70/70 passed independently after `931a2ea0`.
- Immutable PostgreSQL checkpoint `03a726ea`: 128/128 passed, including explicit Keep with locked-commit tampering and unchanged accepted authority.
- Immutable PostgreSQL checkpoint `32402a982dd7`: 132/133 passed. All operation workflows passed. The remaining assertion treated the internal raw job record as a public projection and placed a canary in provisional fiction; `b3e99d54` replaces it with incomplete JSON containing private scratchpad evidence and checks the actual polling/streaming schemas.
- Final candidate PostgreSQL result is recorded below after execution. These are synthetic provider HTTP tests with a real isolated PostgreSQL database, not live model compatibility evidence.
- Independent runtime and final scoped source reviews found no outstanding production-code finding after the evidence-limit correction. Final whole-branch review remains phase 06 work.

Earlier failed checkpoints exposed request-only checkpoint/ledger ordering defects, protected-state merge errors, and fixture defects; those failed runs are not counted as passes. Existing historical rejection behavior remains intact: compatibility means preserving original meanings and hashes, not accepting snapshots which main already rejects.

No live generation, production data change, deployment, push or main integration occurred. Phase 05 adds server-owned status, controls and reporting; phase 06 prepares the bounded DeepSeek compatibility experiment and complete release verification. A concrete-model probe cannot establish compatibility for mutable `@preset/nexus-nsfw` itself.

Final candidate verification: `b3e99d5476b3b807492ffe7c9544967bd09a62f3` passed **133/133 PostgreSQL integration tests across 7 files**, exit 0, using the pinned Linux verifier and a fresh isolated database. Rejection, refusal and incomplete streaming each persisted exact private invocation evidence, made one provider dispatch, retained accepted authority and offered no automatic redispatch. Both public projections excluded the private scratchpad marker while retaining provisional narration. No live provider request was made.
