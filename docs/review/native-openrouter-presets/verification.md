# Native OpenRouter preset verification

## Residual charged-terminal accounting closure (2026-09-21)

The direct world-preview terminal path now reads the same owner-scoped durable request ledger before returning its original error. Only a strict finite aggregate is included as optional `physicalAccounting` on the existing API error envelope for `/api/v1/worlds/generate-preview`; the code, status, retry decision, authoring details and accepted-state rules remain unchanged. A failed aggregate read leaves the original error intact and omits the field. The private logical-attempt accounting attached to a terminal executor error is not mistaken for the whole request.

The exact composed real-PostgreSQL regression failed on the prior production source (`terminal-error-exact-red.txt`, one failure because the API field was absent) and passed after the fix (`terminal-error-green-final-focus.txt`, one pass). A valid charged outline followed by a separately charged invalid-serving-identity seed produces one success and one terminal failure, no route fallback, no saved world, an unknown total-token count, and separate USD/EUR subtotals. The actual HTTP error contains only the finite aggregate and generic safe 500 envelope; a forced aggregate-read fault returns the same error/status/details without a partial logical summary or another dispatch. The completed affected files passed 9/9 authoring-reliability, 31/31 authoring-stage-execution and 8/8 world-generation real-PostgreSQL cases. Five affected unit files passed 108/108 and pinned `pnpm check` passed. These are follow-up selected checks; earlier full-suite, browser and Linux evidence remains tied to its earlier source. The private accounting-remediation report indexes raw logs and final source identity.

## Accounting remediation after scoped review (2026-09-21)

The scoped review of the consolidated fix wave found that its durable physical-attempt ledger was not consumed by the owner-facing authoring job detail or direct world-preview completion. It also found that a failed optional accounting read after a completed provider attempt could replace successful content with an error. A bounded follow-up now projects the existing ledger into those two completion/read paths and makes the post-success logical summary read best-effort. The owner-facing summary contains only finite attempt counts, observed token subtotals and coverage, and cost subtotals separated by currency; missing usage remains unknown. Before the residual closure above, the direct terminal-error path relied on the durable ledger without exposing the aggregate in its error response.

Focused RED logs are `summary-success-red.txt` (one failing unit) and `accounting-production-red.txt` (two failing real-PostgreSQL cases). GREEN evidence: `summary-success-green.txt` (3/3 unit), `accounting-production-green-v2.txt` (2/2 real PostgreSQL), `check-final.txt` (pinned check, 1,836 boundary/data candidates), and `unit-affected-final.txt` (8 affected files, 93/93). The complete selected real-PostgreSQL lane passed 8/8 files, 177 tests passed and 14 Windows platform skips; `pg-<name>.txt` files in the private accounting-remediation log folder hold each raw result. It includes owner-scoped HTTP authoring detail and direct preview completion with multiple logical invocations, partial and absent usage, two currencies, repeated reads, and wrong-owner isolation. The image-pipeline file passed 25 cases and skipped 14 Linux-only cases on Windows. These are selected affected checks, not a full unit/integration/browser/Linux rerun on the follow-up source. The executable/test patch in commit `96e3946c959a32a94c5522f8a9b53bb1b03294ba` against `e925ebad96d4a3618003b84a74eb61ba22dada55` has SHA256 `F8E850924F728298CECB2F9EC060D31B5BF2EEBE40F48649F083D9A2E18CB3EE`, matching its precommit staged bytes. The private `accounting-remediation-report.md` records exact commands and remaining limits. No live inference or activation occurred.

## Final F1–F9 fix wave (2026-09-21)

The consolidated review fixes were verified on executable source diff SHA256 `D184F880DD7DB0ED5B1021811962FF00BE7B6EC638F67DC20B851DB80B69838B` against base `1018c88ff5c519291a3d3333e59e9fbf342fc52c`. The versioned Linux harness digest was `635288DD6C0154DB73F73B80815C01AF917784CDABBB0D7C2AC3C0962362275A`; the final Docker image ID was `sha256:1169d5c89854493b63ad69c9f5b8df39e39e5ee4c5c839ad5166af1b85a7912c`. The older Task8 identities and counts below describe the earlier source only.

| Fix-wave gate | Result |
| --- | --- |
| Pinned `pnpm check` and `pnpm build` | Both exit 0; check inspected 1,835 boundary/data candidates; build retained existing font/chunk advisories. |
| Complete Windows unit suite | 337 files, 4,262 passed, 44 platform skips, zero failures. |
| Selected affected real PostgreSQL integration files | 13 complete files, 313 passed, 14 Windows platform skips, zero failures. The image-pipeline file was rerun on final source (25 pass/14 skips); the other 12 passed on the preceding source, which differs only by the null-parent illustration guard/test. This is not a rerun of the earlier 110-file sweep. |
| Both legacy Chromium suites | 12/12 passed on the preceding source, including actual request cancellation, finite diagnostics, keyboard and mobile label. The final edit did not touch browser code. No live provider call. |
| Final-source Linux affected integration harness | 13 files, 212/212 passed, zero skips. The harness uses the actual compiled service and clients, Chromium, and isolated real PostgreSQL. |
| Final-source Linux platform units | Seven files, 181 passed, one intentional inverse-platform skip, zero failures. |

The first complete final-source unit run had one recovery-panel clean-load assertion fail after two fixed timer ticks (4,261 pass/44 skips); the focused test and then the complete suite passed on unchanged source. The private report preserves that failure.

The private `final-fix-report.md` in the task's SDD folder indexes raw logs, RED/GREEN evidence, exact file inventory, and source identity. The refreshed [mobile Story capture](../assets/native-openrouter-presets/story-mobile-profile.png) shows the inherited policy ahead of a long model ID. The task-owned Linux test network was removed after the run; the task database was retained. The 136 unrelated Linux-gated Windows cases and opt-in baseline/benchmark skips from Task8 remain outside this selected final wave. Neither offline fixture evidence nor these tests establish live OpenRouter compatibility or authorize activation.

## Prior Task8 evidence (earlier source)

Task1–7 approved code base: `6e67cc7a9cc8dd167d37df9cd2f68506a3949fc7`. The broad Task8 gates below used commit `d7e366151c14a6c58c1d5bc8117c5fef6662b827` (its precommit executable diff SHA256 was `217d59c96bfbb726689de213e7a29ef66f8b0408a83f94c49b57aad61e19842c`). A later scoped review fix changed the v2 failure-log error-name projection and added two durable PostgreSQL regressions. Its focused evidence is recorded below; the earlier full unit/PG, browser and Linux image results remain evidence for their exact earlier source, not reruns after this fix. The private Task8 report preserves RED/GREEN and log paths. Independent scoped and whole-branch audits remain pending. Production native admission is off; these results do not establish live OpenRouter compatibility.

All local commands ran from the isolated feature worktree with the repository-pinned pnpm 12.4.1 shim at the front of PATH. PostgreSQL used the dedicated task-owned `infinitequest-native-presets-test` container, an ignored private `TEST_DATABASE_URL`, and the repository's per-file isolated database setup. No production or shared database was reset.

| Gate | Final command/evidence | Result and limit |
| --- | --- | --- |
| Static/contracts/syntax | `pnpm check` | Exit 0; boundary/data check (1,831 candidates), package/web checks, TypeScript with noEmit and JavaScript syntax. |
| Build | `pnpm build` | Exit 0; existing font-resolution/chunk-size advisories, no build failure. |
| Full unit | `pnpm test:unit --exclude '**/.worktrees/**' --exclude '**/.codex/**'` | 337/337 files, 4,255 passed, 44 platform-guarded skips, zero failures/unhandled. Earlier 15-failure Task5C inventory, Story initialization readiness race, and frozen v2 fixture failures were reconciled in this final run. |
| Affected real PostgreSQL | Task-private `node run-task-integration.mjs` invoking the repository's `run-isolated-integration.mjs` per each of 110 discovered files with explicit private `vitest.integration.config.ts` | Exit 0; 101 passed and 9 skipped files, 1,189 passed, 199 skipped, zero failed tests. 192 skips are Linux-gated secure/archive cases; six are opt-in known-failure story-context baselines, one is an opt-in Chronicle historical benchmark. The 12-file earlier failure audit and fixture/production corrections have final green evidence. This is a Windows run, not Linux coverage. |
| Legacy browser | `pnpm exec playwright test tests/e2e/legacy-provider-presets.e2e.test.ts tests/e2e/structured-output-settings.e2e.test.ts --workers=1` | 10/10 passed, no skips. It exercises both legacy entrypoints and typed outgoing payloads with mocked API/model fixtures, desktop/mobile/keyboard interactions, and stale async responses. No live provider call. |
| Linux affected platform integration | `docker build -f docs/review/native-openrouter-presets/linux-harness/Dockerfile -t infinitequest-native-presets-linux:task8-versioned-217d59c9 .`, then `docker run --rm --network <isolated-task-network> --env-file <ignored-task-test-env> infinitequest-native-presets-linux:task8-versioned-217d59c9` | Final versioned harness: 13/13 files, 211/211 tests, zero skips, including actual compiled service/both built clients, Chromium, secure staging, image independence, archive, import-memory and newly gated gameplay/adapter/world-campaign cases. Image ID `sha256:fe776271a137c72deccec46bbcde55ffeeff399ea288917f7ca8127933aed936`; harness digest `e3b2d4389d88dd90cdeb4aec2d8aedd20ec57f67e02de7a7e1a182765b0cdabb`. Build context excludes private SDD config, environment files, logs and scratch. |
| Linux affected platform units | The final image runs seven named platform unit files with repository Vitest | 7/7 files, 181 passed, one inverse unsupported-host skip, zero failures. Earlier missing nonsecret installation doc in image allowlist was corrected and rerun green. |
| Whitespace | `git diff --check` | Required after final docs and before commit; Task8 report records final result. |

The scoped review fix reproduced two v2 failures in the real PostgreSQL response-contract suite: an unsafe `Error.name` appeared in `turn_generation_provider_failed`, and a throwing getter left a reserved invocation `dispatched`. The logger now uses the existing bounded `diagnosticErrorName` projection. Focused RED was 2 failed/42 filtered; focused GREEN was 2 passed/42 filtered; the complete affected response-contract file then passed 44/44 with no skips. `pnpm check` also passed after the fix (1,835 boundary/data candidates). These focused results exercise terminal invocation completion, no accepted mutation, and no private name in the event; they do not relabel the prior broad/Linux image as final-source reruns. The accounting-failure event name remains unchanged pending the whole-branch operational-contract ruling.

The Linux 13-file lane executed 56 cases that the broad Windows suite skipped due to platform (48 original affected cases plus gameplay 1, adapter matrix 6 and world-campaign route 1). It also includes cases that had already passed on Windows, so its 211 total is not 211 newly covered skips. The other 136 Windows Linux-gated cases concern unrelated Task14 asset/publication suites and have not been executed here. The opt-in seven and unsupported-host unit skip also remain skips.

Requirement proof is spread across independently approved task slices and final composed suites. This index gives the final reviewer useful current anchors; passing file counts alone do not settle semantics.

| Requirement | Inspect source and assertions |
| --- | --- |
| Typed selection, Required Model versus trusted Preset, exact capability negative/positive, schema closure | `packages/application/src/providers/response-format.ts`, `packages/contracts/src/provider-output-schema.ts`, `services/runtime/src/generation-response-contract.ts`; `tests/unit/generation-response-contract-operation-matrix.test.ts`, `tests/unit/client-api-routes.test.ts`, provider/configuration PostgreSQL suites. |
| Frozen plans, current authority, migrations, historical worker fences and archive | `database/migrations/0097_*.sql`–`0100_*.sql`, `packages/database/src/generation-execution-repository.ts`; generation, authoring, illustration, migrations, portability and system-archive PostgreSQL suites. |
| Exact body/prompt once, bounded ordered routing, durable attempts, serving identity, two-job isolation | `packages/story-engine/src/preset-route-execution.ts`, `services/runtime/src/generation-executor-adapter.ts`; response-contract/route/workflow and direct-authoring units plus generation PostgreSQL suites. Actual production HTTP Retry-After date/seconds assertion: `tests/unit/providers.test.ts`. |
| All active text consumers and independent nontext | Authoring/source/organizer/illustration consumer units and PostgreSQL suites; `tests/integration/image-pipeline.integration.test.ts`, `story-memory-compatibility.integration.test.ts`; 38 Linux image cases all executed. |
| Overrides, safe APIs/editor and both legacy UI surfaces | `tests/unit/client-api-routes.test.ts`, `tests/unit/story-player-ui.test.ts`, `tests/e2e/legacy-provider-presets.e2e.test.ts`, `structured-output-settings.e2e.test.ts`; actual desktop/mobile captures below. |
| Offline probe | `scripts/lib/structured-output-probe.ts`, `scripts/probe-structured-output.ts`, `tests/unit/probe-structured-output.test.ts`; [hypothetical 17-operation plan](probe-plan.md) and [public primary metadata](probe-metadata-2026-09-20.md). Exact private preset order/price and live schema acceptance are unverified. |

The 11 [versioned browser captures](../assets/native-openrouter-presets/) came from the Task7 approved browser fixture run. Nine remain byte-identical to the reviewed Task7 source captures; `settings-desktop-overrides-explicit.png` and `story-mobile-profile.png` were refreshed from the final passing Task8 browser rerun. The six principal detail/diagnostic captures remained byte-identical. Settings desktop/mobile and Story desktop/mobile details, trusted-Preset and verified-Model diagnostics are represented. The three existing `../assets/structured-output/` baselines were also refreshed by the final browser run. These are rendered fixture evidence, not provider compatibility certification.

The operational [rollout](rollout.md) keeps admission default-off, requires all workers upgraded, and drains/pauses immutable jobs before application rollback. The [acceptance status](acceptance-status.md) and [decisions](implementation-decisions.md) distinguish final task evidence from the pending independent whole-branch review.
