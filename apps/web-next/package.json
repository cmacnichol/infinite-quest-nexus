{
  "name": "@infinite-quest/web-next",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@infinite-quest/client-core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  },
  "scripts": {
    "build": "vite build",
    "check": "tsc --ignoreConfig --noEmit --target ES2023 --module ESNext --moduleResolution Bundler --lib ES2023,DOM --types vite/client,node src/bootstrap.ts vite.config.ts",
    "dev": "vite"
  }
}
