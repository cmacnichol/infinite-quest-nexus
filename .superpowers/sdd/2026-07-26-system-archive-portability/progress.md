# SDD ledger — plan: docs/superpowers/plans/2026-07-26-system-archive-portability.md

Workspace: C:\Users\chris\.codex\worktrees\67e3\InfiniteQuest (linked worktree, detached HEAD, externally managed).
Merge base: 7eed1c524ac3b7fc57d6f0198661830265630cba.
Spec read: docs/superpowers/specs/2026-07-26-portable-campaign-and-system-archives-design.md.

## Pre-flight interface scan

| Tasks / interface | Producer -> consumer | Finding / ruling |
| --- | --- | --- |
| 1 -> 2 / contracts and classification | Task 1 defines archive types and exhaustive table registry; Task 2 introduces new persisted tables. | Ruling: Task 2 must extend the Task 1 classification registry and its inventory test for migrations 0078/0079, although its file list omits those files. The registry's exhaustive invariant in the accepted spec controls. Cost if wrong: new system tables become unclassified or the test fails. |
| 1 -> 3 / record, job, owner types | Task 1 schemas and branded commands -> Task 3 ports/use cases. | Compatible: Task 3 consumes the stable contracts without redefining wire schemas. |
| 2 -> 3 / jobs and artifacts | Durable export jobs and artifact constraints -> export execution. | Compatible: Task 3 must use Task 2 repository/constraints rather than campaign-import operation rows. |
| 2 -> 4 / upload sessions | Upload metadata and limits -> assembly/preview. | Compatible: Task 4 extends private upload publication only. |
| 3 -> 4 / composition | Export composition -> upload/preview additions. | Compatible: Task 4 modifies the same composition; it must retain export behavior. |
| 4 -> 5 / preview authority | Destination fingerprint and staged handle -> atomic import. | Compatible: Task 5 revalidates before the gate and preserves opaque handles. |
| 5 -> 6 / server and runtime | Mutation gate/import execution -> public routes and worker lane. | Compatible: Task 6 registers feature-gated routes after the gate/security ordering. |
| 6 -> 7 / JSON API | Server-owned system routes -> both Data Transfer clients. | Compatible: Task 7 consumes only JSON contracts and retains existing routes/deep links. |
| 7 -> 8 / release evidence | UI and route contracts -> cross-version fixture/E2E. | Compatible: Task 8 verifies, but does not enable, the capability. |
| 8 -> 9 / verified behavior | Final capability evidence -> documentation. | Compatible: Task 9 documents default-off behavior unless a separate evidence-backed gate approval enables it. |
| 2 and 8 / runtime configuration | Task 2 adds default-safe configuration; Task 8 names config as a possible modification. | Ruling: Task 2 owns the configuration additions; Task 8 changes config only if a release-gate evidence correction is required. Cost if wrong: duplicate or contradictory configuration behavior. |
| 7 / legacy HTML scope | Task 7 changes apps/web/public/index.html; global constraint forbids historical root index.html. | Ruling: Task 7 may change apps/web/public/index.html only; repository-root historical index.html remains untouched. Cost if wrong: the active legacy Data Transfer surface would be unavailable or historical reference content would be changed. |

| Task | Internal consistency check |
| --- | --- |
| 1 | Tests and schemas both require strict, secret-free logical records plus a migration-table inventory; no conflict. |
| 2 | Additive migrations, repository behavior, and default-off configuration align; classification follow-up is ruled above. |
| 3 | Snapshot/stream interfaces and deterministic export tests agree on logical records and original assets. |
| 4 | Upload chunk safety, fingerprinting, and opaque preview authority all precede mutation. |
| 5 | Gate exemptions, transactional import ordering, and post-commit rebuilds agree. |
| 6 | Feature-gated routes, fair worker lane, and API-only CLI use the same public application surface. |
| 7 | Both UI surfaces consume server JSON and preserve legacy imports/exports; no client ZIP parsing. |
| 8 | Fixture and round-trip evidence retain default-off behavior; no contradictory enablement step. |
| 9 | Documentation is explicitly downstream of verified behavior and the capability gate. |

Baseline: `pnpm test` was run in the normal sandbox and failed only because the web-build contract could not read its Vite configuration. It was rerun with normal filesystem visibility; the visible unit result was 200 files passed, 2,328 tests passed, 44 skipped before the integration-runner output was truncated. Focused task evidence remains the acceptance gate for each task.

Task 1: dispatched (base 7eed1c5; implementer /root/system_archive_task1).
Task 1: review failed — Critical: payload record envelopes accepted arbitrary JSON rather than domain-specific secret/operational exclusions. Important: `chronicle_chunk_jobs` was rebuildable instead of operational; negative tests missed the payload boundary. Minor: reported focused-test count did not match the reviewed test definitions.
Task 1: fix round 1/5 (3 addressed, 1 open — generic authority payload schema is still a bypassable field-name denylist; commits f8cc33c..8519501).
Task 1: fix round 2/5 (critical closed-boundary finding addressed; 1 Important new-breakage — positive record schemas omit restorable world-version, world-draft, and campaign-state authority; commits 8519501..4b79099).
Task 1: fix round 3/5 (world/draft/state authority added, 1 Important new-breakage — closed world-content schema excludes persisted character profiles and tracker-shaped default triggers; commits 4b79099..1c5f143).
Task 1: fix round 4/5 (character profiles and tracker-shaped defaults addressed, no open Critical/Important findings; commits 1c5f143..ac71180).
Task 1: complete (commits 7eed1c5..ac71180, review clean). Independent verification: direct focused Vitest 19/19 and `pnpm check` passed; PostgreSQL was not applicable to this contract-only task.
Task 2: dispatched (base ac71180; implementer /root/system_archive_task2). Carry-forward ruling: migrations 0078/0079 must extend the central registry as operational tables.
Task 2: review failed — Important: kind/status compatibility could bypass active-job uniqueness; processing jobs could persist without leases; portable-artifact constraint replacement was not online-safe; expired upload status was rolled back. Minor: chunk index bound did not match PostgreSQL integer storage.
Task 2: fix round 1/5 (4 addressed, 1 open — migration runner's single transaction retains the initial ALTER TABLE lock through constraint validation, defeating online-safe replacement; commits cb4ef90..bd88614).
Task 2: fix round 2/5 (phased migration boundaries implemented, 1 Important new-breakage — transaction-control guard misses PostgreSQL aliases/forms and lets ordinary migrations bypass atomicity; commits bd88614..466b182).
Task 2: fix round 3/5 (statement-aware guard added, 2 Important open issues — identifier-adjacent dollar tokens can hide transaction controls, and SQL-standard function bodies misclassify END; commits 466b182..0de6656).
Task 2: fix round 4/5 (two scanner fixes added, 2 Important open issues — `BEGIN ATOMIC` can be activated from a routine header and non-ASCII identifier/tag handling remains narrower than PostgreSQL; commits 0de6656..3fa88fd).
Task 2: fix round 5/5 (routine-header and high-bit lexical defects addressed, no open Critical/Important findings; commits 3fa88fd..b2c6caf).
Task 2: complete (commits ac71180..b2c6caf, review clean). Independent verification: fresh PostgreSQL migration/resumable suite 33/33 and `pnpm check` passed.
Task 3: dispatched (base b2c6caf; implementer /root/system_archive_task3).
Task 3: Ruling: extend the shared archive writer's trusted manifest validation narrowly enough to accept the validated System Archive root manifest, including required owner provenance, rather than omitting it from `manifest.json`. The specification requires root manifest provenance and the existing writer's base-only strict reparse is an integration incompatibility. Cost if wrong: a shared-writer change could affect existing archive formats; mitigate with focused Campaign/World regression coverage and scoped staging.
Task 3: review failed — Critical: root manifest retains only owner count, not source-owner provenance. Important: cancellation can orphan a published artifact; operational-work counts omit active/retryable job types; composition spools archive data in OS temp outside private durable filesystem authority.
Task 3: fix round 1/5 (provenance and operational counts addressed; 2 Important open issues — cleanup errors can turn cancellation/publish into failed or unlinked artifacts, and final ZIP/active scratch expiry lack durable lifecycle authority; commits e1f00ff..62551d3).
Task 3: fix round 2/5 (durable staged ZIP lifecycle, cancellation/publication finality, and active-export expiry renewal addressed; 1 Important open issue — a stalled heartbeat can leave a reader active beyond a DB lease after another reaper acquires cleanup authority; commits 62551d3..a859c3f).
Task 3: fix round 3/5 (DB lease expiry fence and competing-reaper regression added; 1 Important open issue — reader wall-clock skew could outlive the PostgreSQL lease authority; commits a859c3f..750b055).
Task 3: fix round 4/5 (monotonic, conservatively bounded local lease deadline and skew regression added; no open Critical/Important findings; commits 750b055..5130f8a).
Task 3: complete (commits b2c6caf..5130f8a, review clean). Independent verification: focused unit/archive suite 108 passed and 4 Windows-gated skips; PostgreSQL System Archive suite 20 passed and 1 Linux-only skip; `pnpm check` passed. Carry-forward: Linux-only physical inode/reaper behavior was exercised on Linux by the implementer but remains platform-skipped in this Windows worktree; a separately disclosed existing Linux assertion expects a now-absent private `artifacts/` directory and must be triaged before the full System Archive suite is called green.
Task 4: Ruling: expand the shared System Archive preview contract minimally in Task 4, despite the brief's original file list omitting contracts, so the required rich, non-mutating preview has one validated public representation. Cost if wrong: a rich internal preview would be inaccessible or later duplicated/redefined insecurely by the API/UI tasks.
Task 4: review failed — Critical: production durable upload/preview storage ports lack implementations; Important: v1 compatibility metadata is discarded, preview can retain the full archive/images in heap, expiry/fingerprint semantics permit stale or incomplete preflight, strict relationship validation is incomplete, and unknown capacity can be sufficient without an override.
Task 4: fix round 1/5 (durable storage, source compatibility metadata, streaming index, lifecycle/relationship/capacity safeguards addressed; review still found Critical active-upload/preview staging lifecycle loss and Important ambiguous-commit compensation, bounded JSON decoding, and restore-key validation gaps; commits 8bfff29..4188914).
Task 4: fix round 2/5 (lifecycle authority, commit-error reconciliation, pre-allocation metadata bounds, and most restore-key validation addressed; 1 Important open issue — world-draft source identity is not constrained to its `world_id` restore key or uniquely indexed; commits 4188914..fbd348c).
Task 4: fix round 3/5 (world-draft source/restore-key equality and uniqueness addressed; no open Critical/Important findings; commits fbd348c..e7a4ec8).
Task 4: complete (commits 5130f8a..e7a4ec8, review clean). Independent verification: focused unit/archive suite 116 passed and 4 platform skips; PostgreSQL System Archive suite 49 passed and 2 Linux-only physical-filesystem skips; `pnpm check` passed. Carry-forward: Linux-only physical private-filesystem paths remain platform-skipped in this Windows worktree; database durability/interleaving coverage ran.
Task 5: review failed — Critical: campaign-scoped prompt overrides lose scope/collide, narration corrections and Chronicle authority are reconstructed with invented semantics, and import counts can declare success after silently dropping records. Important: authoritative commit lacks a stale-worker fence, commit retry is not idempotent, and durable import report omits required reconciliation/provenance fields.
Task 5: fix round 1/5 (prompt scope, correction/memory authority, record reconciliation, idempotency, initial lease/report work addressed; review still found Critical lossy Chronicle summary checkpoint metadata and Important final commit fencing, report/preview omission categories, asset-binding reconciliation, and `waiting_for_gate` workflow gaps; commits 031aaa0..36f4afd).
Task 5: fix round 2/5 (exact checkpoint authority, categorized reports, binding reconciliation, final fence, and `waiting_for_gate` lifecycle addressed; 1 Important open issue — authoritative transaction locks its job row, blocking external lease renewal and causing valid long imports to self-expire; commits 36f4afd..ff4adb5).
Task 5: fix round 3/5 (final transition renews the same owner fence while retained row/advisory locks prevent stale/competing authority; no open Critical/Important findings; commits ff4adb5..ffa2c0f).
Task 5: complete (commits e7a4ec8..ffa2c0f, review clean). Independent verification: focused import/contract suite 53 passed; PostgreSQL System Archive, resumable, and asset-archive suite 68 passed and 4 platform skips; Task implementer also ran `pnpm build` successfully. Carry-forward: physical secure-staging rollback paths remain platform-skipped on Windows.
Task 6: review failed — Important: CLI confirms before preview and falsely asserts acknowledgements; sparse upload resume is ambiguous; completed/oversized downloads fail on 416; production errors lack stable codes; raw failures can leak private data to logs; worker shutdown can skip archive cleanup; Task 7 lacks a read-only capability-detection contract.
Task 6: fix round 1/5 (six transfer/operational boundary findings addressed; 1 Important open issue — production server hides safe controlled System Archive 5xx codes although route-local test doubles expose them; commits a6a3100..5e53b2f).
Task 6: fix round 2/5 (production allowlisted sanitized 5xx System Archive codes and buildServer regression addressed; no open Critical/Important findings; commits 5e53b2f..1d39828).
Task 6: complete (commits ffa2c0f..1d39828, review clean). Independent verification: Task6 implementer ran 237 focused/boundary tests with 13 Windows skips plus `pnpm check` and `pnpm build`; independent final server review ran 48/48 focused tests. Carry-forward: PostgreSQL runtime and physical filesystem routes remain unverified in the Task6 environment and must be covered by Task8 release evidence.
Task 7: Ruling: rendered browser tests that intercept Task6 JSON establish UI contract behavior only; real built-service, PostgreSQL, and archive-root round-trip proof is Task 8's explicit release gate. Cost if wrong: mocked UI evidence could be mistaken for a cross-service import/export guarantee.
Task 7: review failed — Important: durable jobs cannot reconnect after reload, cancellation leaves stale preview authority, required preview/report evidence is omitted from both surfaces, and mocked Vite tests are not built-service/PG proof; Minor: transfer progress phases lack accessible naming/live announcements.
Task 7: fix round 1/5 (durable recovery, evidence presentation, progress semantics addressed; 1 Important open issue — terminal import A masks newer ambiguously accepted B during cancellation; commits cc2f166..504d2db).
Task 7: fix round 2/5 (current/stored import identity correctly replaces prior terminal state and cancellation recovers/cancels ambiguous B; no open Critical/Important findings; commits 504d2db..f676b49).
Task 7: complete (commits 1d39828..f676b49, review clean). Independent verification: implementer ran 110 focused unit tests, 17 mocked JSON-contract Playwright tests, `pnpm check`, and both web builds; final independent cross-client ambiguous-cancel regression passed 2/2. Per ruling, real built-service/PostgreSQL/private-archive proof remains Task 8.
Task 5: Ruling: the user explicitly authorized one additional repair round after the five-round breaker because final Task 8 evidence exposed two load-bearing preview relationship defects. This exception is limited to current-state reconciliation and migration/transfer scoping, followed by one independent scoped re-review. Cost if wrong: the workflow cap is weakened and further review churn could obscure whether the design itself needs revision.
Task 5: extra fix round 6/6 dispatched — open Critical: normal accepted-turn campaign revisions without a same-revision edit are rejected; open Important: migration/transfer relationships can be cross-wired to unrelated campaigns/world versions.
Task 5: extra fix round 6/6 (both load-bearing findings addressed; normal accepted-turn state reconciles without fabricated edits, and migration/transfer campaign-world-version relationships fail closed; commits c251bfd..fec43bb).
Task 5: extra round complete (commit fec43bb, independent scoped review clean). The user-authorized breaker exception is consumed; no further Task 5 repair round is implied.
Task 8: current-head evidence refresh after Task 5 repair — `pnpm check` passed; strict fixture 1 passed/2 Linux-only skipped; repaired preview 32/32 and PostgreSQL authority 37 passed/3 Windows skips. Release gate remains default-off and blocked by composition parity, Task 7 CSS policy, pre-durable-handle cancellation, one Docker/PostgreSQL expiry poll, and two unexecuted Linux runtime scenarios.
Task 7: fix round 3/5 (pre-durable-handle export cancellation now resolves the persisted idempotent job before server cancellation on both clients; palette-policy leak replaced with approved token; commit 72a3d5c; independent review clean).
Task 4: fix round 4/5 (System Archive storage now consumes the named `ApiAssetComposition["storage"]` seam while preserving one secure runtime instance; commit 1d3f5d0; independent review clean).
Task 8: current-head full-gate refresh — unit gate 2 failed/2489 passed/44 skipped, both Task 7 cancellation regressions; strict fixture 1 passed/2 Linux-only skipped; PostgreSQL expiry 2 failed/19 passed due application/database clock divergence; `pnpm check` passed. Release remains default-off.
Task 7: fix round 4/5 (known durable export/upload cancellation precedence repaired; review found 3 Important concurrency gaps; commit 15c2e2b).
Task 7: fix round 5/5 (legacy handle cleanup, single-flight cancellation, and abort-safe replacement monitoring addressed; 1 load-bearing Important remains — same-kind recovery A can overwrite newer ambiguous operation B before cancellation; commit e3207a5).
Task 7: parked — same-kind reload recovery can overwrite a newly started operation and cause cancellation to target A while B remains running — Ruling: the finding is real and load-bearing, but Task 7's five-round breaker has tripped; carry it verbatim into the single whole-branch final fix wave rather than silently accepting it or exceeding the task loop again. Cost if wrong: an ambiguously accepted newer export may remain active while the UI reports an older export cancelled; release must remain default-off until the final fix is reviewed.
Task 7: complete (commits 1d39828..e3207a5, 1 parked load-bearing finding routed to final fix wave).
Task 8: complete as default-off release evidence (commits f676b49..8f0e0fb plus current-head report refreshes). Windows evidence: strict fixture 1 passed/2 Linux-only skipped; full unit gate was later reduced to the parked Task 7 race path; PostgreSQL expiry remains environment-blocked by observed Docker clock discontinuity. Capability is not approved for enablement.
Task 8: fix round 4/5 (compiled Linux release service Preview TTL aligned to the fixed 1,800-second authority with an unconditional drift assertion; commit eec496f; independent review clean).
Task 9: fix round 1/5 (six documentation accuracy findings addressed; commit d57db79; independent review found one Minor only).
Task 9: minor (deferred): environment configuration wording should clarify that `ARCHIVE_PREVIEW_TTL_SECONDS` remains configurable for Campaign Archives while System Archive requires exactly 1,800 seconds.
Task 9: complete (commits e3207a5..d57db79, review clean except 1 deferred Minor).
Final whole-branch review: NOT READY. Critical: field-incomplete/lossy authority model; destructive post-import Chronicle rebuild. Important: parked same-kind UI generation race; OS-temp private preview index; System/Campaign TTL coupling plus mixed clocks; transforming text validation. Minor: TTL documentation wording. One consolidated final fix wave dispatched per workflow.
Final fix wave: commits fb4bac9..c384a39. Reported verification: `pnpm check`/build green; unit 2504 passed/44 skipped; all 68 PostgreSQL integration files exited green; Playwright 27/27; Linux-only physical filesystem/process-death paths unexecuted on Windows.
Final scoped re-review: ADDRESSED — destructive Chronicle rebuild, both-client operation-generation fencing, independent System TTL/database-clock expiry, non-transforming exact text, TTL documentation, and removal of OS-temp preview files.
Final scoped re-review: residual Critical — v2 generic JSON/mechanics/URL fields accept nested secrets/capabilities — Ruling: real and load-bearing; the single final fix wave has been consumed, so keep System Archive default-off and require a new explicitly authorized remediation cycle. Cost if wrong: credentials, capabilities, or private provider material may enter an unencrypted archive or imported authority.
Final scoped re-review: residual Critical — node-postgres timestamp conversion truncates PostgreSQL microseconds — Ruling: real and load-bearing for exact portability; keep release blocked until timestamp strings are extracted/persisted without precision loss. Cost if wrong: source and destination authority differ despite successful hashes/reports.
Final scoped re-review: residual Critical — bigint activity IDs are converted through JavaScript Number and reject/lose valid values above MAX_SAFE_INTEGER — Ruling: real and load-bearing; retain decimal strings through SQL binding in a future authorized remediation. Cost if wrong: valid activity authority cannot be imported exactly.
Final scoped re-review: residual Important — in-memory SQLite preview index is row-bounded but lacks a byte/heap bound under very large allowed archives — Ruling: real and load-bearing for hostile/untrusted input; release stays default-off until bounded storage or explicit memory accounting is implemented and reviewed. Cost if wrong: a validly bounded ZIP can still exhaust API/worker memory during Preview.
Final status: NOT READY / default-off. No second final fix wave dispatched because the approved subagent workflow permits exactly one; residual load-bearing findings are surfaced to the user for renewed authority.
Second final remediation cycle: Ruling: the user explicitly authorized another fix round after reviewing the four residual blockers. This exception is limited to (1) strict secret/capability rejection across recursive JSON, mechanics, and image URL boundaries, (2) exact PostgreSQL timestamp extraction without JavaScript Date precision loss, (3) decimal-string preservation for PostgreSQL bigint activity IDs, and (4) an explicit byte/heap bound for the in-memory preview index. System Archive remains default-off pending fresh security and consolidated re-review. Cost if wrong: widening this exception could conceal unrelated redesign work or weaken the completed task review boundaries.
Second final remediation implementation: complete in `36e77b1` (`Fix System Archive residual blockers`). Strict RED/GREEN evidence is recorded in `second-final-fix-report.md`. Final implementation-head gates passed: focused contracts/preview 82/82; PostgreSQL System Archive 41 passed/4 Windows platform skips; `pnpm check`; `pnpm build`; full unit 211 files with 2,519 passed/44 skipped; complete isolated PostgreSQL runner 68/68 files, exit 0; Data Transfer Playwright 27/27. A preceding complete-run attempt stopped once on an unrelated finalized-delivery concurrency assertion at file 42/68; the exact test and whole file immediately passed, and the clean full restart completed. System Archive remains default-off pending the controller's fresh security/consolidated review; Linux-only private staging, resumable recovery, metadata-backfill, process-death, and private-root paths remain unverified on this Windows host.
Second final remediation security review: NOT CLEAN. Confirmed High bypasses: punctuation-separated secret aliases such as `api.key`, `private.key`, `auth.header`, and `bearer.grant`; explicit v2 cost-event `providerResponseId` remained portable; percent-encoded temporary/signed image path segments bypassed the raw-path check. Ruling: these are direct instances of the user-authorized secret/capability blocker, so return them to the same consolidated implementer for one narrow test-first repair before the four-finding review. Cost if wrong: an unencrypted System Archive could still disclose provider or access authority despite otherwise passing validation.
Second final remediation post-review security repair: complete in `6c7eece` (`Harden System Archive authority boundaries`). Strict RED captured 14 failed/46 passed contract-registry probes plus 2 failed/43 filtered PostgreSQL cases before production edits; focused GREEN passed 60/60 and 2/2 respectively. Current-head gates passed: `pnpm check`; `pnpm build`; full unit 211 files with 2,532 passed/44 skipped; System Archive PostgreSQL 41 passed/4 Windows platform skips; complete isolated PostgreSQL runner 68/68 files, exit 0; Data Transfer Playwright 27/27. The v2 boundary now rejects rather than transforms provider-response authority, export/import cannot emit or restore it, v1 compatibility remains, Unicode/punctuation secret aliases and encoded signed/temp image paths fail closed, and safe story JSON/public image controls pass. System Archive remains default-off pending the controller's consolidated re-review; previously listed Windows/Linux-only paths remain unverified.
