Wait, the Github Actions CI output says that 6 tests failed in integration.
Specifically, `provider_cost_events` violating NOT NULL constraint:
```
error: null value in column "owner_user_id" of relation "provider_cost_events" violates not-null constraint
```
Wait! I didn't push my last commit `Revert to NO ACTION for compound keys replacing ON DELETE SET NULL` to origin!
Wait! The github actions logs in the prompt were BEFORE my last commit.
Actually, the last github actions logs we received are from exactly when I made the `Fix ON DELETE SET NULL constraint syntax in multi tenant hardening` commit, which caused `SET NULL (turn_id)` to NOT work properly (or rather, the OLD ON DELETE SET NULL on turn_id was deleted by my DROP CONSTRAINT IF EXISTS). Wait!
If I dropped `provider_cost_events_turn_id_fkey`, which is the old one, AND I changed the new one to `ON DELETE SET NULL (turn_id)` - maybe Postgres 15's `ON DELETE SET NULL` doesn't cascade ONLY `turn_id`? Or it does, but wait! The issue with my NO ACTION fix:
If `provider_cost_events` has NO ACTION on `turn_id, campaign_id, owner_user_id` but the OLD constraint `ON DELETE SET NULL` was NOT DROPPED, wait - but I *did* drop it originally.
If I don't drop it, I have to know the name.

But actually, I can just use `ON DELETE SET NULL` on the single `turn_id` column explicitly:
```sql
ALTER TABLE provider_cost_events
  DROP CONSTRAINT IF EXISTS provider_cost_events_turn_id_fkey,
  ADD CONSTRAINT provider_cost_events_turn_id_fkey FOREIGN KEY (turn_id) REFERENCES turns(id) ON DELETE SET NULL,
  ADD CONSTRAINT provider_cost_events_turn_owner_fk FOREIGN KEY (turn_id, campaign_id, owner_user_id) REFERENCES turns(id, campaign_id, owner_user_id);
```
Yes! This will drop any existing FK, re-create the `turn_id` ON DELETE SET NULL, AND add the compound `NO ACTION` validation key!
This is perfectly safe and explicitly does exactly what we want without relying on the exact name of the previously generated constraint or Postgres 15's `SET NULL (column)` feature, which might be why `provider_cost_events` still threw the NOT NULL constraint! (Actually, maybe the NOT NULL error was triggered because `campaign_id` and `owner_user_id` WERE set to NULL in Postgres 18 because `SET NULL (turn_id)` isn't supported or I had a syntax error that node-pg-migrate swallowed? No, `SET NULL (turn_id)` is valid, but maybe it set all of them?)

Let's modify `0019_multi_tenant_hardening.sql` to re-create the `turn_id` single foreign keys `ON DELETE SET NULL` explicitly.

Wait! The existing single FKs were added in prior migrations. For example, in `0015_campaign_cost_events.sql`:
`turn_id uuid REFERENCES turns(id) ON DELETE SET NULL` -> The name generated by Postgres is `provider_cost_events_turn_id_fkey`.
So I can drop that name, then add the single FK back with that name, AND add the compound FK!
Wait, if I just keep the original one, I don't need to drop it at all!
BUT in my previous commits I dropped them:
`DROP CONSTRAINT IF EXISTS provider_cost_events_turn_id_fkey` etc.
Since my previous commit already pushed to the branch:
Wait, I amended the commit. But github actions already ran on the PREVIOUS push. So I can just force push my amended commit.

Wait, in my amended commit (`Revert to NO ACTION for compound keys...`), I did NOT add back the single-column `turn_id` ON DELETE SET NULL constraints. I just added the compound keys WITHOUT `ON DELETE` actions, but REMOVED the `DROP CONSTRAINT IF EXISTS` statements.
So the original single-column `ON DELETE SET NULL` constraints from `0015_campaign_cost_events.sql`, `0009_...`, etc. are STILL THERE.
If they are still there, they will correctly nullify `turn_id`.
Then the new compound constraint `FOREIGN KEY (turn_id, campaign_id, owner_user_id) REFERENCES turns(...)` has no `ON DELETE` action. When `turn_id` becomes NULL, the constraint passes because of `MATCH SIMPLE`.

Wait, are the names of the old constraints identical to what I was trying to drop?
If I just keep my amended commit and force push, will it work?
Let's review the amended `0019_multi_tenant_hardening.sql`:
```sql
ALTER TABLE activity_events
  ADD CONSTRAINT activity_events_campaign_owner_fk
  FOREIGN KEY (campaign_id, owner_user_id) REFERENCES campaigns(id, owner_user_id) ON DELETE CASCADE;
```
For `activity_events`, `campaign_id` also has `ON DELETE CASCADE`. If I just add the compound FK with `ON DELETE CASCADE`, when a campaign is deleted, BOTH constraints say CASCADE. So it's safe.

What about `imports`?
```sql
ALTER TABLE imports
  ADD CONSTRAINT imports_world_owner_fk
  FOREIGN KEY (world_id, owner_user_id) REFERENCES worlds(id, owner_user_id),
  ADD CONSTRAINT imports_world_version_owner_fk
  FOREIGN KEY (world_version_id, owner_user_id) REFERENCES world_versions(id, owner_user_id),
  ADD CONSTRAINT imports_campaign_owner_fk
  FOREIGN KEY (campaign_id, owner_user_id) REFERENCES campaigns(id, owner_user_id);
```
Wait, originally in `0001_initial_nexus.sql`:
`world_id uuid REFERENCES worlds(id)` -> This has NO ON DELETE action. So it defaults to NO ACTION.
So adding compound FKs with NO ACTION is identical.

What about `assets`?
```sql
ALTER TABLE assets
  ADD CONSTRAINT assets_campaign_owner_fk
  FOREIGN KEY (campaign_id, owner_user_id) REFERENCES campaigns(id, owner_user_id) ON DELETE CASCADE,
  ADD CONSTRAINT assets_turn_owner_fk
  FOREIGN KEY (turn_id, campaign_id, owner_user_id) REFERENCES turns(id, campaign_id, owner_user_id);
```
Original for `assets`:
`campaign_id uuid REFERENCES campaigns(id) ON DELETE CASCADE`
`turn_id uuid REFERENCES turns(id) ON DELETE SET NULL`
My amended file has `ON DELETE CASCADE` for the compound `campaign_id`, and NO ACTION for the compound `turn_id`.
This is exactly correct. When a turn is deleted, the original FK sets `turn_id` to NULL. The compound FK sees `turn_id` as NULL and accepts it.

What about `asset_references`?
Original: `FOREIGN KEY (turn_id) REFERENCES turns(id) ON DELETE SET NULL`
My amended file: `FOREIGN KEY (turn_id, campaign_id, owner_user_id) REFERENCES turns(id, campaign_id, owner_user_id)` NO ACTION.
This is exactly correct.

`chronicle_memories`:
Original: `turn_id uuid REFERENCES turns(id) ON DELETE CASCADE`
My amended file: `FOREIGN KEY (turn_id, campaign_id, owner_user_id) REFERENCES turns(id, campaign_id, owner_user_id) ON DELETE CASCADE`
This is exactly correct.

`generation_jobs`:
Original: `result_turn_id uuid REFERENCES turns(id) ON DELETE SET NULL`
My amended file: NO ACTION compound FK.
Exactly correct.

`provider_cost_events`:
Original: `turn_id uuid REFERENCES turns(id) ON DELETE SET NULL`
My amended file: NO ACTION compound FK.
Exactly correct.

So my amended commit is PERFECT. I just need to `git push --force origin HEAD`.
Wait, let's verify if my local docker sandbox failed because of my AMENDED commit, or because of missing docker Postgres container?
The local `pnpm test:integration` failed because `docker compose up -d postgres` failed because of the overlayfs issue in my sandbox, so there was NO postgres running.
The failure was `connect ECONNREFUSED 127.0.0.1:5432`.
So my amended commit is fully sound and should fix the github actions issue.
Let's just push it.
